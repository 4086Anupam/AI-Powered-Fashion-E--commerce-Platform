import { createTheme } from "@mui/material";

const customeTheme = createTheme({
  palette: {
    mode: "light",
    primary: {
      // main:"#01594c"
      main: "#0694bf",
    },
    secondary: {
      main: "#EAF0F1",
    },
  },
});

export default customeTheme;
